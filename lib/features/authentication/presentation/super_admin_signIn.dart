import 'package:flutter/material.dart';

class SuperAdminSignin extends StatelessWidget {
  const SuperAdminSignin({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Center(
          child: Text("Super admin"),
        ),
      ),
    );
  }
}
